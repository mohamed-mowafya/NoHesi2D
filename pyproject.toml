[project]
name = "NoHesi2D"
version = "0.1.0"
requires-python = ">=3.11"

dependencies = [
    "pygame==2.6.1",
]

[project.optional-dependencies]
dev = [
    "black==25.9.0",
    "mypy==1.18.2",
    "pytest==8.4.2",
]

[dependency-groups]
dev = [
    "pytest>=8.4.2",
]
